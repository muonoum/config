#!/opt/homebrew/bin/fish

for dir in $HOME/{{ .source }}/*
    if string match --quiet '*.skip' $dir
        continue
    end
    echo -n "$(basename $dir): "
    git -C $dir stash && git -C $dir pull && git -C $dir stash pop
end

brew update

hx --grammar fetch
hx --grammar build
